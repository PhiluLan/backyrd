# Slice 3 Recommendation and Open Decisions

Recommended Slice 3: policy calibration and adapter certification, still before broad UI migration.

1. Approve source classes and verification processes per high-risk attribute family.
2. Approve TTL/reverification classes (not arbitrary universal durations).
3. Decide public email classification and public-contact consumer contract.
4. Define spot identity merge/archive ledger semantics.
5. Certify value-level adapters for Restaurant Information V1.
6. Run an authorized, aggregate-only production coverage profile.
7. Design persistent append-only claim/source/verification storage plus RLS/ACL separately.
8. Specify a Decision adapter that consumes policy-authorized World facts; capability-to-intent remains a Decision Intelligence registry owned and approved separately.

Open Founder/CTO decisions: source hierarchy by fact type; accepted verification processes/verifier roles; TTL class assignment and durations; conflict selection for sensitive facts; public email visibility; mapping of legacy categories outside the explicit allow-list; legacy price-level retirement; exact merge/duplicate authority; whether any Gold accepted facts qualify as referenced without re-observation.

Deferred: database migration/storage, RLS/grants, production profiling, Admin/Owner UI, mobile/web wiring, Decision vNext runtime, capability-to-intent mappings, rankings, full 741 taxonomy, final TTLs and universal source priority.
