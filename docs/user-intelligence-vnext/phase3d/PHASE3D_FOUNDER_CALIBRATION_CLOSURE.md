# Phase 3D – Founder Calibration Closure

## Append-only Identität

Der frühere Record `backyrd.user-intelligence.calibration-decisions@3d-1` mit Hash `95cb5372a7a490067f9c614d6b834d22b0bd308149994b2a257eff8d4be98cc7` bleibt unverändert. Der neue Record `backyrd.user-intelligence.calibration-decisions@3d-2` verweist auf diese Identität und bindet alle fünf Founder-Auswahlen. Änderungen benötigen einen neuen versionierten Record und einen separat akzeptierten Release-/Trust-Anchor.

## Ausgewählte Kalibrierungsregeln

1. Contextual Search: drei unabhängige, semantisch passende Journeys; Context bleibt erhalten.
2. Long-Term Search Readiness: sechs unabhängige Eigennutzer-Journeys; Reise-/Fremdsuche ist ausgeschlossen, Rohtext wird nicht gespeichert und contextloser Taste bleibt verboten.
3. Skip Maturity: drei unabhängige Journeys für Subject × Spot × versionierte Decision-Aufgabe × Context; technische Decision-IDs bleiben Provenance. Das Ergebnis ist nur eine sehr schwache situationsgebundene Hypothese.
4. Concept Taste: drei unterschiedliche Spots und drei qualifizierte unabhängige Experiences mit explizitem gerichtetem Outcome und sicherer Event-time-World-Attribution. Der erste Zustand bleibt begrenzt und unsicher.
5. Retention: getrennte Klassen für minimierte Search-, Skip- und Context-Daten, Evidence Ledger, abgeleitetes Modell, Correction Guard und nachweislich nicht personenbezogenen Audit. Konkrete Fristen sind `NOT_CONFIGURED_PENDING_LEGAL_AND_CTO_APPROVAL`.

## Autorisierungsgrenze

Alle fünf Einträge tragen `FOUNDER_SELECTED`, `CALIBRATION_APPROVED` und `PRODUCT_ACTIVATION_NOT_AUTHORIZED`. Der Record, Release und Trust Anchor setzen `productionAuthorized`, `runtimeActivated`, `rankingAuthorized`, `eligibilityAuthorized` und `shadowTrafficAuthorized` auf `false`. Die Phase-3C-Product-Policy und Signal Registry bleiben unverändert. Es entstehen keine Migration, Function, Runtime-Anbindung oder Production-Ausführung.
