# Live Admin World Knowledge search candidate

Status: **Draft PR; Production NO-GO.** Base: `991f644ad7a66eb511ef752f66447b16d4b32228` (the canonical Main after PR #315). This is a `DATABASE_PR` with Admin, World and Decision consumers and a Product-release preflight change. No Production action is authorized by this document.

## Established versus unobserved

- The GitHub/Vercel deployment metadata records a successful `backyrd-intelligence` Production deployment sourced from Main `991f644ad7a66eb511ef752f66447b16d4b32228`. It does **not** prove the custom-domain alias, its current HTTP status, or its database RPC result.
- The shipped `/world-knowledge` Product Admin correction has only a Spot-ID input. Its **Spot laden** action invokes `world_product_authoring_detail_v1`; an untouched page does not automatically make that detail call. The local Founder panel and `world_founder_list_spots_v1` are shown only for localhost Supabase configuration. Its local evaluation set is not the Production catalog.
- The reported generic error at `intelligence.backyrd.ch` has **not** been reproduced as a measured HTTP failure in this task; Production was not queried. Missing World-Authoring RPCs in Production are the reported operational condition, not an independently measured response code. Do not attribute the visible error to a particular status or RPC until the protected read-only preflight captures it.
- The canonical Production catalog source in code is `public.spots` with `status = 'approved'`. The new `world_product_admin_search_spots_v1` returns only `spotId`, `name`, `city`, and `hasMore`, bounded to 20 results in the Admin route (30 maximum in SQL); no Founder cohort, private claims, contacts, actor data or service key is returned. The route first checks `auth.getUser` and `admin_is_admin_v1`, and the SQL RPC independently checks `auth.uid()` and Admin authority.
- The existing append-only Product Admin claim, authority check, rebuild, canonical World reader and Decision Product context are **reused**, not forked. Local PostgreSQL-17 tests assert discovery and the existing claim→rebuild→reader path. The SQL-backed Decision rehearsal then evaluates the rebuilt World snapshot. These are local/prod-like results, not Production proof.

## Fail-closed behavior

The Admin UI distinguishes a missing search RPC/migration (`World Knowledge hier noch nicht verfügbar`), unavailable service, denied Admin authority, and a genuine empty approved-spot match. The search query and correction inputs remain in memory on failed calls. Open claim conflicts block editing of affected fields. Product authority OFF and Emergency-OFF still block the existing write and Decision evaluation paths. The search endpoint itself never grants Product ON authority or table access to a browser.

## Source-aware release decision — all steps require separate authorization

1. Certify the final Draft-PR head, tree, migration bytes, database clean-boot fingerprints, Node-20 artifact, Product Manifest v3 and source-aware Production plan. All required GitHub Risk Gate checks must pass. Keep `executionAuthorized: false`; no Production action follows merely from CI green.
2. After a separately authorized merge, verify exact canonical Main, POST_MERGE_MAIN manifest, build-once/deploy-the-same-artifact identity and post-merge gates. Production remains NO-GO pending the remaining checks.
3. In a protected **read-only** Production preflight, capture the exact custom-domain `/world-knowledge` request/response including the specific failing call and status (without tokens or personal data); compare Production migration ledger against the **13** pending candidate migrations, currently published `decision-v13` Function/app source sets, installed mobile version, and a usable recovery point. Reject drift, missing evidence or an unobserved Admin route. The prior 12 migrations are immutable; this PR adds only `20260919090423_world_product_admin_spot_search_v1.sql`.
4. Only with a separate Production release GO, restore-drilled recovery point and Emergency-OFF readiness: apply the exact forward-only migration set in lineage order, verify grants/RLS and RPC availability, deploy the already certified Function and Admin artifact in the approved order, and keep Product runtime OFF until explicit activation authority. No rebuild of a different artifact.
5. Smoke with a real authorized Admin on the actual Admin domain: find a known approved Spot by name, open it, submit a safe append-only correction, verify Product authority, rebuild and canonical reader value, and verify that a new Decision request sees it. Also check non-Admin denial, true empty result, missing/backend-down messaging, conflict preservation, reload and Emergency-OFF. Stop on any mismatch; do not relabel a browser mock or local fixture as live proof.
6. Only after the separately authorized Mobile OTA, perform installed-device Decision and Admin→Mobile smoke, monitor errors and authority state. A new native binary is not expected from this code-only Admin/SQL change, but installed-app compatibility remains a release preflight condition. On failure, engage Emergency-OFF, stop writes/evaluations, return to the recorded prior Function/Admin/OTA source sets as applicable, and use the tested database recovery procedure rather than manually reversing immutable migrations.

## Stop conditions

Any failed required gate; missing/mismatched migration, Function, app, artifact or recovery identity; unavailable or unauthorized real Admin read/write; hidden conflict; reader/Decision drift; inability to activate Emergency-OFF; or an installed-device mismatch blocks release. No Production query, migration, Function deployment, Admin deployment, runtime activation or OTA was performed for this candidate.
