# World/Admin 48h Production cutover — prepared, not authorized

Status: **PREPARED_NOT_EXECUTED**. This runbook is the World Knowledge and existing Admin Dashboard handoff to Integration. It is not a deployment authorization.

## Frozen scope

The database scope is exactly ten forward migrations in repository order: the nine sealed World Knowledge migrations plus `20260918123000_founder_live_durable_idempotency_v1.sql`. Their paths, bytes, SHA-256 hashes, PostgreSQL 17 compatibility, `SECURITY DEFINER` search paths and source-aware order are generated by:

```sh
npm run world-knowledge:production-cutover:plan
```

No migration is created or edited by this track. A second application must be a migration-ledger `NO_OP`. The full source-aware repository plan also sees `decision-founder-live`; this World/Admin track does not deploy it. It remains an Integration-owned, default-OFF function candidate. Auth deployment is false.

## Release boundary

- Existing Mobile remains the only product surface.
- Existing Admin Dashboard remains the only Admin authoring surface.
- Decision reads World data only through `WorldKnowledgeReaderPort@1.0` and the sanitized public projection. Direct ledger/table reads and client-derived authority are prohibited.
- Real allowlist membership is zero. Authority, expiration and retention are `NOT_CONFIGURED`.
- Default is OFF; the kill switch remains `ENGAGED`.
- The only enabled rehearsal environments are `LOCAL_TEST` and `PROD_LIKE_TEST`.
- Production connections, queries, writes and product outputs performed by this track are all zero.

## Integration-owned execution sequence

Integration must bind the final canonical Main and tree, rebuild the source-aware plan, and require byte-identical migration order and hashes before any Production authorization is considered.

1. Verify the exact source SHA/tree, plan hash and ten migration hashes.
2. Create a logical backup and prove that the backup is readable before applying anything.
3. Apply the ten migrations once, in order, through the canonical migration workflow.
4. Verify migration ledger, RLS, grants, views, functions, empty `search_path`, public projection sanitization and durable idempotency.
5. Exercise Admin write → server authority → append-only claim → rebuild → canonical reader → reload on an explicitly authorized test Spot.
6. Exercise the canonical reader over the frozen Founder cohort and a bounded Basel sample while preserving known, false, unknown, disputed, not-applicable, not-configured, stale and expired states.
7. Hand the verified reader and Admin evidence to Integration while runtime routing remains OFF.

Stop on any hash, order, backup, RLS/grant, reader-contract, sanitization or Admin-roundtrip mismatch. Do not continue partially.

## Recovery

Database rollback is restore/forward-fix based; destructive down-migrations are not provided. Before apply, abort safely. After apply, keep runtime OFF, engage the kill switch, retain append-only history, and use a reviewed forward fix or the verified restore procedure. A failed Admin or reader smoke blocks activation but must not mutate historical claims.

## Required rehearsal

After source freeze, run exactly one PostgreSQL 17 disposable clean boot with all ten migrations, all World SQL authorization suites, the real rebuild-concurrency test, second-apply `NO_OP`, and logical backup/restore. Then run exactly one complete World/Admin regression. Integration owns the later cross-domain and Production gates.

No Production database, credential, user, Spot, migration, function, auth setting, OTA or deployment is touched by this runbook.
