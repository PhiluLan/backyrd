# Unified User Learning Validation

Validated fixtures cover:

- pinned Passt to Unified Fusion and bounded Card nodes;
- 8 concepts × 4 scopes without multiplying one independent session;
- SELF_DECLARED plus Passt through the unchanged bounded formula;
- two Decision sessions counting as two, not 64;
- later contradictory review evidence without an irreversible positive ratchet;
- no fabricated comparative observation, visit, satisfaction, or durable negative;
- unpinned history remaining fail-closed;
- pinned-envelope rebuild stability when current N4 changes;
- worker retry and response-loss reconciliation;
- Mobile neutral Weiter and foreground-duration Exposure contract;
- service-only persistence and additive lineage schema.

The real Production fixture is evaluated read-only before deployment and is not UID-special-cased. No synthetic event is inserted for backyrdBuddy.
