# Canonical Alignment V1 Validation

## Result

The shared registry and additive migration connect Product producers to the existing frozen engine without changing the 45 Taste Concepts or 60 N4 dimensions.

Validated chains:

- `gemütlich/cozy` → `vibe.cozy`; unsupported/test mood → non-qualifying.
- `ruhig/leise/quiet` → current intent → quiet N4 candidate → deterministic WHY_NOW.
- “Regentag mit meiner 4-jährigen Tochter” → N3 V2 rain + family + age 4 → source-bound Spot facts → factual ranking → authorized reasons.
- Date/afterwork remain current context, not durable preference.
- All 14 live categories use one adapter; unknown is explicit.
- Self-declared Profile/Onboarding evidence uses the frozen N5.6 onboarding weak prior, is labeled `DECLARED`, supports removal, and never directly becomes durable/HIGH.
- N6 accepts a factual reason only as the exact candidate-specific authorized code/reference pair.
- Legacy/fixture N4 and review inputs are excluded from canonical learning.

## Verification performed

- Clean local Supabase migration reset through `20260821220000_create_canonical_semantic_alignment_v1.sql`.
- Canonical DB/RLS conformance test passed transactionally.
- Existing Basel Gold, Gold Authoring, Product→N2, N4 adapter, User Intelligence runtime/execution, Decision Input and deterministic orchestrator SQL regressions passed transactionally.
- 58 Shared Registry, Decision Input, Orchestrator, N6 Shadow and User Intelligence Node tests passed.
- 9 transactional SQL suites passed (new canonical conformance plus 8 existing Gold/N2/N4/User Intelligence/Decision regressions).
- Mobile lint completed with zero errors (pre-existing warnings only); Admin and Owner target lint passed; Admin and Web production builds passed.
- No online research, fake facts, fake reviews, historical rewrite, Production deployment, or registry expansion occurred.

## Live-query semantic proof

For `Regentag mit meiner 4-jährigen Tochter`, the Product runtime now preserves:

- rain = `PREFERRED`, provenance `EXPLICIT` from request text;
- family context = `FAMILY_WITH_CHILD`, provenance `EXPLICIT`;
- child age = `4`, provenance `EXPLICIT`;
- all three are soft Current Request facts, not durable Taste or automatic hard exclusions.

Where accepted Spot facts exist, the package carries exact source identities for family, age, environment and rain. A candidate with `age 3–8`, `INDOOR`, and rain/family `SUITABLE` receives factual matches. Missing facts remain unknown and unpunished. This validates semantic preservation, not final Basel content quality.

## Compatibility

Historical records retain their original versions. The mobile onboarding call moves to `complete_decision_onboarding_v2`; favorite UX remains, while the old synthetic Decision, ML and legacy Taste rebuild are absent from the new path. The Admin and Owner editors render the server-authorized Gold catalog instead of maintaining surface-local field lists. Production is unchanged until the additive migration and server/mobile artifacts are deployed.
