export function markdownReport(result) { return `# Decision Quality Run\n\n- Framework validity: **${result.frameworkValidity}**\n- D3 readiness: **${result.d3Readiness}**\n- Constitution: \`${result.freeze.constitutionHash}\`\n- Scenarios: ${result.splitIntegrity.counts.DEVELOPMENT} development / ${result.splitIntegrity.counts.REGRESSION} regression / ${result.splitIntegrity.counts.LOCKED_HOLDOUT} locked holdout\n- D0-F-002: ${result.acceptance.knownEngineDefect.status}\n\n## Non-claims\n\nFAST_SIMULATION is not Production latency or real-outcome evidence.\n`; }
