# Legacy pre-baseline migrations

These SQL files are immutable historical evidence from the pre-baseline migration chain.
They are intentionally outside `supabase/migrations/` and must never be executed automatically.

The active chain is the five remotely registered foundation migrations followed by the canonical baseline.
